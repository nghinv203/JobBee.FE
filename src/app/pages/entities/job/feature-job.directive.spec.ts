import { FeatureJobDirective } from './feature-job.directive';

describe('FeatureJobDirective', () => {
  it('should create an instance', () => {
    const directive = new FeatureJobDirective();
    expect(directive).toBeTruthy();
  });
});
